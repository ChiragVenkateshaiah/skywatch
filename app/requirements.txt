# The Databricks Apps base image already ships streamlit + databricks-sdk; pinned
# here so local runs and the deployed app resolve the same versions.
streamlit>=1.38,<2
databricks-sdk>=0.30
pandas>=2.0
altair>=5.2
