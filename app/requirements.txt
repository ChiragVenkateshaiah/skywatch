# The Databricks Apps base image already ships streamlit + databricks-sdk; pinned
# here so local runs and the deployed app resolve the same versions.
streamlit>=1.38,<2
databricks-sdk>=0.30
pandas>=2.0
altair>=5.2
pydeck>=0.9
# click-to-predict loads the model straight from a Volume (joblib) — no mlflow/UC
# registry access needed, since the per-model EXECUTE grant isn't available on this
# metastore. See app/model.py.
joblib>=1.3
lightgbm>=4.3
